const config = {
    apiBaseUrl: 'https://your-api-base-url',
    storageKeys: {
        authToken: 'auth_token',
        userData: 'user_data',
        isAdmin: 'is_admin'
    }
};

export default config;
